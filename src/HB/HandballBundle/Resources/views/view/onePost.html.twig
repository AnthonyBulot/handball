{# empty Twig template #}
{% extends "HBHandballBundle::layout.html.twig" %}

{% block body %}
    <div>
            <p>{{ post.title}}</p>
            <p>{{ post.content}}</p>
            <p><img src="{{ asset(post.image.uploadDir ~ '/' ~ post.image.id ~ '.' ~ post.image.url) }}" alt='{{ post.image.alt }}' /></p>
            <p>{{ post.category.name}}</p>
            
    </div>

{% endblock %}           
